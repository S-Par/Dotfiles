export PATH=/usr/local/smlnj/bin:"$PATH"

export PROMPT="[%F{blue}%n%f] : %F{cyan}%~%f %# "

if command -v pyenv 1>/dev/null 2>&1; then
  eval "$(pyenv init -)"
fi

export NVM_DIR="/Users/siddharthparmar/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"  # This loads nvm
